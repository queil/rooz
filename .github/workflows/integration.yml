name: integration

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  CARGO_TERM_COLOR: always

jobs:
  # ── unit tests (no daemon) ──────────────────────────────────────────────────
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-
      - name: Unit and doc tests
        run: cargo test --lib --bins

  # ── integration tests (daemon required) ────────────────────────────────────
  integration:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        engine: [docker, podman]
      fail-fast: false

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Build binary
        run: cargo build

      - name: Start ${{ matrix.engine }} daemon
        run: |
          bash scripts/test-daemon.sh up ${{ matrix.engine }} \
            | grep '^export ' \
            | sed 's/^export //' \
            >> "$GITHUB_ENV"

      - name: Integration tests
        run: |
          cargo test \
            --test smoke \
            --test lifecycle \
            --test volumes \
            --test sidecars \
            --test networks \
            --test interactive \
            -- --test-threads=1

      - name: Stop daemon
        if: always()
        run: bash scripts/test-daemon.sh down ${{ matrix.engine }}
